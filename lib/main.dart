import 'package:flutter/material.dart';
import 'package:sante/HomePage.dart';
import 'package:sante/bar.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Prfil(),
    );
  }
}
